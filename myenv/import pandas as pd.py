import pandas as pd
print(pd.__version__)
# for Creating Virtual Environment
# python -m venv venv
# venv\Scripts\activate
# pip install -r requirements.txt it is carry all the packages with it's versions
# to install all version use following cmds:
# pip install -r requirements-dev.txt
# pip install -r requirements-prod.txt
# pip freeze> requirement.txt it is for display the packages used